# 重要设计决策

> 只记录影响工程边界、结构或后续实现方式的重要决策：什么决策、为什么、什么时候做的。

## 决策记录

### 1. 当前阶段只做工程初始化，不开发业务功能

- 决策：本阶段聚焦项目骨架、环境配置和可验证测试框架；暂不实现会议、任务、文件、部件追溯等业务。
- 原因：先建立可编译、可测试、可启动的工程基线，避免在基础设施未稳定时引入业务复杂度。
- 时间：2026-08-25。

### 2. 保持模板模块、坐标和包名不变

- 决策：继续使用现有 `xiaou-*` 模块名、Maven 坐标和 `com.xiaou` 包名，不在初始化阶段重命名或重构模板职责。
- 原因：减少无关变更，保持现有依赖关系和启动方式稳定，便于后续按切片增量开发。
- 时间：2026-08-25。

### 3. 本地敏感配置通过环境变量提供

- 决策：数据库、Redis、密钥和远程账号不写死在代码或提交的配置中；使用 `.env.example` 说明配置项。
- 原因：避免敏感信息泄露，并让本地、测试和部署环境可以分别注入配置。
- 时间：2026-08-25。

### 4. 按单一可验收切片推进

- 决策：同一时刻只处理一个功能点；一个切片完成数据库、后端、前端、测试和验证闭环后，再进入下一个切片。
- 原因：控制变更范围，让每次完成状态都有明确验收标准和真实命令证据。
- 时间：2026-08-25。

### 5. 完成状态必须有本地验证证据

- 决策：只有在本地命令实际通过后，才能将构建、测试、启动或功能标记为已完成；无法执行验证时明确记录为未验证。
- 原因：区分静态检查与真实运行结果，避免把“看起来正确”误报为“可用”。
- 时间：2026-08-25。

### 6. F00 首页路由契约

- 决策：保留 `IndexController` 提供 `GET /`，静态首页必须返回 UTF-8 HTML，并展示 `欢迎使用AECP` 与 `会议 → 任务 → 文件 → 部件追溯`。
- 原因：F00 的可验收行为是可直接访问的首页契约；用 MockMvc 和真实 HTTP 冒烟同时覆盖状态码、响应类型和关键文案。
- 时间：2026-08-25。

### 7. F02 使用 BCrypt 校验和 Redis opaque session

- 决策：F02 后端使用配置注入的演示账号，密码通过 BCrypt 校验；登录生成密码学随机 opaque token，Redis 仅保存 token 到用户名的映射并设置正 TTL；退出删除会话且对重复退出幂等。
- 原因：在不引入数据库用户表、JWT 刷新和前端改造的前提下满足登录、即时失效和本地可验证的最小闭环；F01 前端 demo-session 保持不变。
- 时间：2026-08-25。

### 8. F03 使用独立 Identity 模块、Flyway、Spring JDBC、组织行锁和软删除

- 决策：F03 数据与领域规则放入 `xiaou-aecp-identity` 模块；Flyway V1 创建组织、账号和成员关系；Repository 使用 Spring JDBC；成员删除只置为 inactive 并保留历史行。
- 原因：隔离身份与组织边界，保持 SQL 和领域规则可独立测试；软删除支持重新激活并保留审计语义。
- 并发约束：所有成员写操作先锁定 active 组织行，再在同一事务内重新鉴权和检查最后管理员数量，防止并发降级或删除唯一管理员。
- 迁移约束：已执行的 V1 不修改；后续修复只新增迁移版本。应用回滚保留 V1 表和成员数据。
- 时间：2026-08-26。
